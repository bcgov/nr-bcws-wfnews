/**
 * Generates a rounded rectanglar path
 *
 * @export
 * @param x, y, w, h, r, tl, tr, bl, br
 */
export declare function roundedRect(x: number, y: number, w: number, h: number, r: number, [tl, tr, bl, br]: boolean[]): string;
