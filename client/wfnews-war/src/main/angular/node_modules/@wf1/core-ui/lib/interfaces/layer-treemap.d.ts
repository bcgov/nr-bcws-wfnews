/**
 * Used by MapService to manage map instances
 */
export interface LayerNode {
    title: string;
    id: string;
    isVisible: boolean;
}
export interface TreeNode {
    title?: string;
    isVisible?: boolean;
    mixed: boolean;
    parent?: any;
    child?: TreeNode[];
    lyr?: LayerNode;
}
