import { IncidentType, ReportOfFireType } from "../../application-icon/application-icon.model";
export interface ListItemConfig {
    icon: string;
    iconColourCode?: IncidentType | ReportOfFireType;
    iconIsBlinking?: boolean;
    iconIsCancelled?: boolean;
    iconIsAssignedToIncident?: boolean;
    time?: string;
    title: string;
    info: Info[];
    location: [number, number];
}
export interface Info {
    label: string;
    value: string | number;
    emphasis?: boolean;
    fullWidth?: boolean;
}
