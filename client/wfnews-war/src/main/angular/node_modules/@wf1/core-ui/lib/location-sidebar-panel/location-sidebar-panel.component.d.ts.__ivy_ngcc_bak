import { EventEmitter } from "@angular/core";
import { BasicSidebarPanelComponent } from "../basic-sidebar-panel/basic-sidebar-panel.component";
export declare class LocationSidebarPanelComponent extends BasicSidebarPanelComponent {
    set location(location: [number, number]);
    selectedLocation: [number, number];
    locationSelect: EventEmitter<[number, number]>;
    isInitializing: boolean;
    locationSelected(coordinates: [number, number]): void;
}
