import { SearchState } from "../search/store/state";
import { Injector } from "@angular/core";
/**
 * Function needed to run the application config service at app initialization time.
 */
export declare function appInitializerFn(injector: Injector): () => Promise<void>;
export declare const selectCurrentSearchParam: (prop: any, param: any) => (state: SearchState) => any;
