import { AfterViewChecked, OnInit, TemplateRef } from "@angular/core";
import { TokenService } from "../services/token.service";
import { MapControl } from "../application-toolbar/application-toolbar.component";
import { AppConfigService } from "../services/app-config.service";
export declare class WFMLMappingContainerComponent implements OnInit, AfterViewChecked {
    private token;
    private appConfig;
    titleText: string;
    menuTemplate: TemplateRef<any>;
    toolbarTemplate: TemplateRef<any>;
    sidebarTemplate: TemplateRef<any>;
    mainContentTemplate: TemplateRef<any>;
    showToolbar: boolean;
    opened: boolean;
    isInitializing: boolean;
    constructor(token: TokenService, appConfig: AppConfigService);
    ngOnInit(): void;
    ngAfterViewChecked(): void;
    toggleSideNav(): void;
    initializeMap(): void;
    onMapControlToggled(currentMapControl: MapControl): void;
}
