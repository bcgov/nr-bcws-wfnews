
//This will get overwritten at build time (check mvn-client.yml file).  Defaults to "local" for developer machines, will use Github
//build number in other environments.
export const BUILD_NUMBER = 'local';
