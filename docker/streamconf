stream {

  upstream postgres {
    server {wfnews_db_url}:5432;
  }

  server {
    listen 8080 so_keepalive=on;
    allow all;
    server_name _;

    location /healthCheck {
      return 200;
    }

    location / {
      proxy_pass postgres;
    }
  }
}