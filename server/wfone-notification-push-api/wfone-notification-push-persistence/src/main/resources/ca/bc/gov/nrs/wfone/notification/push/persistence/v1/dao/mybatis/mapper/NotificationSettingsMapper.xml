<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
				namespace="ca.bc.gov.nrs.wfone.notification.push.persistence.v1.dao.mybatis.mapper.NotificationSettingsMapper">

				<!-- One UPDATE for a whole FCM batch, instead of one for each failed token. -->
				<update id="clearDeviceTokens">

								UPDATE NOTIFICATION_SETTINGS
								SET NOTIFICATION_TOKEN = '',
								REVISION_COUNT = REVISION_COUNT + 1,
								UPDATE_USER = COALESCE ( #{userId, javaType=java.lang.String, jdbcType=VARCHAR,
								mode=IN} , current_user),
								UPDATE_TIMESTAMP = current_timestamp
								WHERE SUBSCRIBER_GUID IN
								<foreach item="subscriberGuid" collection="subscriberGuids" open="(" separator=","
												close=")">#{subscriberGuid}
								</foreach>

				</update>


</mapper>
